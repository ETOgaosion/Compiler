// test user-defined function call

int add(int n)
{
    if(n == 2)
        return n * 2;
    else
        return n + 10;
}

int main()
{
    int n, result;
    n = get_int();
    if(n > 1)
        result = add(n);
    else
        result = 1 + 2 * 8 + 100 - 9999 + 10000/5;
    print_int(result);
    return 0;
}
